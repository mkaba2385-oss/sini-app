[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "PYTHONPATH=src alembic upgrade head"
startCommand = "PYTHONPATH=src uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10